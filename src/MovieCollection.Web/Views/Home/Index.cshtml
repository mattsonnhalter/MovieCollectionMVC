@model MovieCollection.Web.Viewmodels.MovieIndexViewModel

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@{
    ViewBag.Title = "Home";
}
<div class="row">
    <div class="col-md-12" style="height:500px;"></div>
</div>
<div class="row">
    <div class="col-md-4 col-md-offset-4">
        @using (Html.BeginForm())
        {
            <div class="form-group">
                @Html.TextBoxFor(model => model.searchModel.Title, new { @class = "form-control input-lg", @placeholder = "Search...", @id = "searchTitleId" })
                @Html.ValidationMessageFor(model => model.searchModel.Title)
            </div>
            <input type="button" value="Submit" class="btn btn-secondary center-block" />
        }
    </div>
</div>

<div class="row">
    @if (Model.movieModels != null)
    {
        foreach (var item in Model.movieModels)
        {
            <ul class="list-unstyled">
                <li class="col-md-4">@item.Title</li>
                <li class="col-md-4">@item.ReleaseDate</li>
                <li class="col-md-4">
                    @if (item.ImageUrl != "http://image.tmdb.org/t/p/w185/")
                    {
                        <img src="@item.ImageUrl" />
                    }
                    else
                    {
                        <img src="~/Images/No-Image.png" />
                    }
                </li>
            </ul>
        }
    }
</div>